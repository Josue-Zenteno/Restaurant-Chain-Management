package A01.RestaurantChainManagement.domain;

public class Waiter {
	
	//TODO Implementation in next iteration

	public static boolean advanceState(int TableID,int maxStateTime) {
			//TODO Implementation in next iteration
			// 
			//	   When state is 8 (In Preparation), the state needs to be 0 again so:
			//
			//		if(t.getState() == 8) {
			//			t.setState(0);  								// Setting state to free again 
			//			for (int i = 0; i < t.getTransactionList().length; i++) {
			//				t.getTransactionList()[i] = null;
			//			}
			//			t.doTransaction(time);
			//			t.update();
		
		
		throw new UnsupportedOperationException();
		
		
	}
}